<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nam.namdemoapp">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".ui.ActivityRecycler">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ui.ActivityLegacy" />
    <activity android:name=".ui.ActivityLinear" />
    <activity android:name=".ui.ActivityWeight" />
    <activity android:name=".ui.activityTable" />
    <activity android:name=".ui.ActivityGrid" />
    <activity android:name=".ui.ActivityColor" />
    <activity android:name=".ui.ActivityRecyclerView" />
    <activity android:name=".ui.ListActivity" />
    <activity android:name=".ui.DetailActivity"></activity>
  </application>

</manifest>